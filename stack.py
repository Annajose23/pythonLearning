#LIFO

#stack 
browsing_session = []
#use append to add item on top of the stack
browsing_session.append(1)
#use pop to remove the item from the top of the stack
browsing_session.pop()
#check if items there in stack
if not browsing_session:
    browsing_session[-1] #return the item at the top of the stack